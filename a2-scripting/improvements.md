after reading the peer feedback, we recieved some ideas for improvement. We'll list them below.
-consider removing the link to header in nav, as header is right after nav
-moving the footer out of main


further we noticed our header having a border radius, causing some white pixels to be exposed
we might also consider adding more comments to our code for clarity.
Beacause our website is dark-themed, we forgot to change the written text's color in the inputs to white. the standard is black.
set the overscroll-behaviour to be none.

WCAG 1.1 – Text Alternatives
-All images include alt text.


WCAG 1.3 – Adaptable
-The page uses semantic HTML elements suchas header, nav, main, section, form, and label.

WCAG 1.4 – Distinguishable

-Sufficient color contrast is used between text and background throughout the page. Font sizes are readable, content does not rely solely on color to convey meaning, and focus indicators are clearly visible for interactive elements.

WCAG 2.1 – Keyboard Accessible

All functionality is accessible using a keyboard. Buttons, links, form inputs, filters, and the modal dialog can be operated using the Tab and Enter keys.
The modal can be opened and closed without using a mouse

WCAG 2.4 – Navigable

-The page includes clear navigation for the user. Aria labels are also implemented for screen-readers.